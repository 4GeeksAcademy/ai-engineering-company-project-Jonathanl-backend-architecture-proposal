// Boilerplate for application form validation logic.

(function () {
  const form = document.getElementById("application-form");

  if (!form) {
    return;
  }

  form.addEventListener("submit", function (event) {
    event.preventDefault();
    // TODO: implement validations.
  });
})();
